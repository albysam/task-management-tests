/**
 * Tasks module test exports.
 * 
 * Test files:
 * - create-task.e2e-spec.ts  - POST /tasks
 * - get-tasks.e2e-spec.ts    - GET /tasks
 * - get-task.e2e-spec.ts     - GET /tasks/:id
 * - update-task-status.e2e-spec.ts - PATCH /tasks/:id/status
 * - delete-task.e2e-spec.ts  - DELETE /tasks/:id
 * 
 * To add new task tests:
 * 1. Create a new file: <action>-task.e2e-spec.ts
 * 2. Use BaseTest or createBaseTest from '../setup'
 * 3. Import helpers from '../helpers'
 * 4. Import fixtures from '../fixtures'
 * 5. Use shared scenarios from '../shared' for common patterns
 */

// Export any task-specific test helpers here

