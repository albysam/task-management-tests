/**
 * Auth module test exports.
 * 
 * Test files:
 * - signup.e2e-spec.ts - User registration tests
 * - signin.e2e-spec.ts - User authentication tests
 * 
 * To add new auth tests:
 * 1. Create a new file: <feature>.e2e-spec.ts
 * 2. Use BaseTest or createBaseTest from '../setup'
 * 3. Import helpers from '../helpers'
 * 4. Import fixtures from '../fixtures'
 * 5. Use shared scenarios from '../shared' for common patterns
 */

// Export any auth-specific test helpers here

